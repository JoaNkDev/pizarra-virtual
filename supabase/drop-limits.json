{
  "query": "drop index if exists public.persona_words_ip_key; alter table public.persona_words drop constraint if exists persona_words_user_id_key; alter table public.persona_words alter column user_id drop not null; alter table public.persona_words drop column if exists ip; drop function if exists public.submit_word(text);"
}
